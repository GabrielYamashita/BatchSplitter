id: standard
name: Standard Template
active: true

output:
  file_prefix: STANDARD

# This example assumes the project.yaml already defines the standard fields:
# - nome
# - telefone
#
# Because this template does not override fields, it inherits the project defaults.
